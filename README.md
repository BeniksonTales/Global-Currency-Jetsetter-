# Global-Currency-Jetsetter-
The user enters an amount and selects a base currency. The app fetches real-time exchange rates from the API and converts the amount into selected target currencies using the formula.
